<h1>Orders#confirm</h1>
<p>Find me in app/views/orders/confirm.html.erb</p>


<div class="container" style="margin-top: 20px;">
 <h4>注文情報確認</h4>
 	<span style="color:#2d714c;">表示価格は全て税込です。</span>
<div class="row">
	<div class="col-xs-8">
		<table class="table table-bordered">
			<tr>
				<th>カートの中身</th>
				<th>商品小計</th>
				<th>送料</th>
				<th>請求金額</th>
			</tr>

		<% @items.each do |item| %>
			<tr>
				<td><%= item.product.name %></td>
				<td><%= addTax(item.product.price) %></td>
				<td><%= item.quantity %></td>
				<td><%= addTax(item.price) %></td>
			</tr>
		<% end %>
		</table>
	</div>

	<div class="col-xs-4">
		<table class="table table-bordered">
			<tr>
				<th>送料</th>
				<td>800</td>
			</tr>
			<tr>
				<th>商品合計</th>
				<td><%= addTax(@items.sum(:price)) %></td>
			</tr>
			<tr>
				<th>請求金額</th>
				<td><%= addTax(@items.sum(:price))+800 %></td>
			</tr>
		</table>
	</div>
</div>

<h4>支払い方法</h4>
	<p><%=# @order.payment_method %></p>


<h4>お届け先</h4>
	<p>〒<%#= @orderparams.postal_code %>
	<%#= @orderparams.address %>
	<%#= @orderparams.last_name %> <%#= @orderparams.first_name %> 様</p>

<hr>

<div class="text-center">
	<%= link_to '注文を確定する', orders_thanks_path, class:"btn btn-success" %>
</div>
</div>
