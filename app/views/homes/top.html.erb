
</br>

<h2>新着商品が
					<%= @count %>
				<span>件あります</span></h2>


<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>商品名</th>
      <th>商品画像</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @Products.each do |product|%>
      <tr>
        <td><%= link_to product.name, product_path(product.id) %></td>
        <td><%= link_to product_path(product.id) do %>
          <%= attachment_image_tag product, :image, style: "width: 200px;" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

</br>

<h2><%= link_to '商品一覧ページはこちら', products_path %></h2>







